import { describe, it, expect } from 'vitest';
import { useFPS } from './useFPS';

describe('useFPS', () => {
  it('should be defined', () => {
    expect(useFPS).toBeDefined();
  });

  // TODO: Add robust unit tests for useFPS
});
