import { describe, it, expect } from 'vitest';
import { useCRDT } from './useCRDT';

describe('useCRDT', () => {
  it('should be defined', () => {
    expect(useCRDT).toBeDefined();
  });

  // TODO: Add robust unit tests for useCRDT
});
