import { describe, it, expect } from 'vitest';
import { useCSPViolation } from './useCSPViolation';

describe('useCSPViolation', () => {
  it('should be defined', () => {
    expect(useCSPViolation).toBeDefined();
  });

  // TODO: Add robust unit tests for useCSPViolation
});
