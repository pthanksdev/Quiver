import { describe, it, expect } from 'vitest';
import { useHMAC } from './useHMAC';

describe('useHMAC', () => {
  it('should be defined', () => {
    expect(useHMAC).toBeDefined();
  });

  // TODO: Add robust unit tests for useHMAC
});
