import { describe, it, expect } from 'vitest';
import { useSorting } from './useSorting';

describe('useSorting', () => {
  it('should be defined', () => {
    expect(useSorting).toBeDefined();
  });

  // TODO: Add robust unit tests for useSorting
});
