import { describe, it, expect } from 'vitest';
import { useHotkey } from './useHotkey';

describe('useHotkey', () => {
  it('should be defined', () => {
    expect(useHotkey).toBeDefined();
  });

  // TODO: Add robust unit tests for useHotkey
});
