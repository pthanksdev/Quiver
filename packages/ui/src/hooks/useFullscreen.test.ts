import { describe, it, expect } from 'vitest';
import { useFullscreen } from './useFullscreen';

describe('useFullscreen', () => {
  it('should be defined', () => {
    expect(useFullscreen).toBeDefined();
  });

  // TODO: Add robust unit tests for useFullscreen
});
