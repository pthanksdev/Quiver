import { describe, it, expect } from 'vitest';
import { useMediaQuery } from './useMediaQuery';

describe('useMediaQuery', () => {
  it('should be defined', () => {
    expect(useMediaQuery).toBeDefined();
  });

  // TODO: Add robust unit tests for useMediaQuery
});
