import { describe, it, expect } from 'vitest';
import { useUndoable } from './useUndoable';

describe('useUndoable', () => {
  it('should be defined', () => {
    expect(useUndoable).toBeDefined();
  });

  // TODO: Add robust unit tests for useUndoable
});
