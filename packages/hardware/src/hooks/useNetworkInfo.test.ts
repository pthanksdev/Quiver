import { describe, it, expect } from 'vitest';
import { useNetworkInfo } from './useNetworkInfo';

describe('useNetworkInfo', () => {
  it('should be defined', () => {
    expect(useNetworkInfo).toBeDefined();
  });

  // TODO: Add robust unit tests for useNetworkInfo
});
